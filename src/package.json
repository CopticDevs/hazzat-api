{
  "name": "hazzat-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "prereq": "npm run init",
    "build": "npm run prereq && tsc -p tsconfig.json && gulp build",
    "build-trace": "npm run prereq && tsc -p tsconfig.json --diagnostics --listFiles --listEmittedFiles --traceResolution",
    "prestart": "npm run build",
    "start": "(cd ../dist && node app)",
    "init": "npm install --no-color --no-optional",
    "sql-server": "docker run -e 'ACCEPT_EULA=Y' -e 'SA_PASSWORD=Pa$$w0rD' -e 'MSSQL_PID=Express' -p 1433:1433 -d mcr.microsoft.com/mssql/server:2017-latest-ubuntu"
  },
  "description": "hazzat-api",
  "author": {
    "name": "Fady H. Wahba"
  },
  "dependencies": {
    "debug": "^2.2.0",
    "express": "^4.14.0",
    "gulp-typescript": "^5.0.1",
    "inversify": "^5.0.1",
    "mssql": "^5.1.0",
    "nconf": "^0.10.0",
    "q": "^1.5.1",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@types/debug": "0.0.30",
    "@types/express": "^4.0.37",
    "@types/mime": "^1.3.1",
    "@types/mssql": "^4.0.14",
    "@types/nconf": "^0.10.0",
    "@types/node": "^8.10.48",
    "@types/q": "^1.5.2",
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-uglify": "^3.0.2",
    "ts-node": "^8.2.0",
    "tslint": "^5.16.0",
    "typescript": "^3.5.1"
  },
  "engines": {
    "node": "^8.0.0"
  }
}
